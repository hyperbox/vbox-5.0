apply plugin: 'base'

evaluationDependsOnChildren()

subprojects {
    apply plugin: 'java'

    sourceCompatibility = 1.7
    targetCompatibility = 1.7

    group = 'io.kamax'
    version = 'latest'

    repositories {
        mavenLocal()
        mavenCentral()
    }
}
